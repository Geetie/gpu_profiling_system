You are a GPU performance analysis expert. Analyze the following Nsight Compute profiling output.

Iteration: {iteration}
Target Metric: {target_metric}

Profile Output:
{profile_output}

{metrics_instruction}

================================================================================
KEY METRIC DEFINITIONS (CRITICAL FOR CORRECT ANALYSIS):
================================================================================

1. sm__throughput.avg.pct_of_peak_sustained_elapsed
   - Measures: Percentage of peak sustained SM compute throughput achieved
   - Higher is BETTER (should be > 50% for compute-bound workloads)
   - If this is LOW (< 30%), the kernel is NOT compute-bound
   - Causes of low values:
     * Grid size too small (not enough blocks to saturate all SMs)
     * Insufficient arithmetic operations per thread
     * Memory-bound instead of compute-bound
     * Poor occupancy (not enough warps active)
   - To IMPROVE: increase grid size, add more FMA operations, use double precision

2. gpu__compute_memory_throughput.avg.pct_of_peak_sustained_elapsed
   - Measures: Percentage of peak sustained GPU compute memory throughput
   - Higher is BETTER (should be > 30% for memory-intensive workloads)
   - If this is LOW (< 20%), memory access patterns are inefficient
   - Causes of low values:
     * Non-coalesced memory access
     * Working set too small (hitting L2 cache instead of DRAM)
     * Too few memory operations relative to compute
     * Poor memory alignment
   - To IMPROVE: use coalesced access, increase working set size (> 128MB), ensure sequential access patterns

================================================================================
ANALYSIS TASKS:
================================================================================

1. IDENTIFY BOTTLENECK TYPE (choose one):
   - compute_bound: high sm__throughput (> 50%), low memory throughput
   - memory_bound: high gpu__compute_memory_throughput (> 30%), low compute throughput
   - low_occupancy: both metrics low, insufficient parallelism
   - poor_configuration: grid/block size issues, wrong kernel parameters
   - optimization_missing: no FMA, no shared memory, no loop unrolling

2. PROVIDE KEY METRICS with specific numerical values from the profile output
   - Quote actual numbers, not just descriptions
   - Compare achieved vs peak values

3. Suggest SPECIFIC CODE IMPROVEMENTS for next iteration:
   - Must include actionable fixes (e.g., "change grid size from X to sm_count*4")
   - Prioritize by expected impact

4. Recommend additional NCU metrics to collect for deeper analysis

================================================================================
POSSIBLE BOTTLENECKS:
================================================================================

- Compute-bound: high compute utilization (> 50%), low memory throughput
- Memory-bound: high memory throughput (> 30%), low compute utilization  
- Low occupancy: insufficient parallelism, both metrics low
- Poor kernel configuration: incorrect grid/block size
- Missing optimization: no FMA usage, no shared memory, wrong precision

================================================================================
RESPONSE FORMAT (JSON ONLY):
================================================================================

{
  "bottleneck": "type",
  "key_metrics": "explanation with specific numerical values from profile output",
  "code_improvements": ["fix1 with specific details", "fix2", "fix3"],
  "recommended_metrics": ["ncu_metric1", "ncu_metric2"],
  "next_benchmark_type": "description of benchmark for next iteration"
}

================================================================================
CRITICAL: The code_improvements field must include specific fixes like:
================================================================================

- "Use sm_count*4 blocks instead of hardcoded grid size"
- "Change float to double precision for all calculations"
- "Add percentage calculation: pct = (achieved/peak)*100"
- "Use clock64() inside __global__ kernel, not in host code"
- "Increase working set size to > 128MB to bypass L2 cache"
- "Use coalesced memory access patterns for better throughput"
- "Add FMA operations: result = a * b + c for better compute utilization"
